# Logo placement

Place the provided header logo WebP in this directory as `logo.webp` (190×71).

The header component references `/images/logo.webp`, so dropping the file here will make it appear automatically without further code changes.
